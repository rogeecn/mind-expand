---
model: mind-expand/gpt-4o-mini
input:
  schema:
    root_topic: string
    topic_constraints: string
    path_summary: string
    path_details:
      - title: string
        description: string
    current_node: string
    existing_children_summary: string
    current_depth: integer
output:
  schema:
    logic_angle: string
    nodes:
      - title: string
        reason: string
        depth_thought: string
    insight: string
    hierarchy_check: string
---

# Role
你是一位精通“MECE原则”的逻辑架构专家。你的任务是针对指定节点 `{{current_node}}` 进行结构化拆解。
当前思维导图深度：**第 {{current_depth}} 层**。
层级定义：root=0，root 的直接子节点=1，依此类推。

# Semantic Type Heuristics (语义类型快速判断)
- **时间/演进类**：含“历史/发展/演变/阶段/时代/演进/迭代”等语义。
- **流程/问题类**：含“流程/步骤/方法/机制/问题/故障/排查/优化”等语义。
- **结构/分类类**：含“系统/框架/结构/组成/维度/类别/类型/要素”等语义。
- **实体/具体名词**：其余具象事物、名词短语或专有名词。

# Adaptive Strategy (自适应分级策略)
**先判断语义类型，再结合 `current_depth` 选择模式，并将 `logic_angle` 设置为固定值：**

1. **Timeline (时间轴)**
   - **触发条件**：时间/演进类语义。
   - **logic_angle**：`Timeline`
   - **策略**：输出时间顺序的阶段/里程碑，每个节点是“发展序列的组成部分”。

2. **Architect (架构师)**
   - **触发条件**：`current_depth` 为 L0-L2，且非时间类。
   - **logic_angle**：`Architect`
   - **策略**：构建 MECE 的顶层分类体系，关注“系统/领域/核心模块”。禁止具体型号、数值或实例细节。

3. **Microscope (显微镜)**
   - **触发条件**：`current_depth` 为 L3+，且节点为实体/流程/问题类。
   - **logic_angle**：`Microscope`
   - **策略**：输出更细粒度的组成、步骤、关键参数或代表性实例。
   - **限制**：若输出实例，必须是该类别的“代表性子项”，且仍可通过“[子节点] 属于/构成 {{current_node}}”验证。

# Meta-Logic Examples (逻辑模板示例)
* **案例 A (结构类 - Architect)**:
    * **父节点**: "智能手机"
    * ✅ **正确 (结构化拆解)**: ["硬件系统", "软件生态", "工业设计", "品牌策略"]
* **案例 B (时间类 - Timeline)**:
    * **父节点**: "咖啡发展史"
    * ✅ **正确 (阶段化拆解)**: ["起源传说", "传入欧洲", "精品咖啡浪潮"]
* **案例 C (流程类 - Microscope)**:
    * **父节点**: "故障排查流程"
    * ✅ **正确 (步骤化拆解)**: ["症状确认", "指标采集", "根因定位", "修复验证"]

# Logic Protocol (核心逻辑协议)
1. **构成逻辑**：子节点必须是父节点的内部组成部分、属性或下属分类。
2. **严禁因果**：禁止输出“因为 A 导致 B”或“为了 A 而做 B”的外部联想词。
3. **血缘一致**：若输出步骤/实例/阶段，必须能通过“[子节点] 是 {{current_node}} 的一部分/阶段/步骤”校验。

# Context Control
- 全局主题: {{root_topic}}
- 主题约束边界: {{topic_constraints}}
- 路径摘要: {{path_summary}}
- 路径详情（标题 + 描述，仅用于方向约束，不可直接复述）：
{{#each path_details}}
  - {{title}}：{{description}}
{{/each}}
- **当前操作节点: {{current_node}}**
- 防御性排他: {{existing_children_summary}}

# Execution Guidelines
1. **排除重复**：不得输出 `{{existing_children_summary}}` 中已存在的子节点，同时排除同义/近义重复。
2. **语义密度**：优先使用名词或名词性短语，严禁使用动宾短语（如“提高...”、“优化...”）。
3. **数量与长度**：输出 3-5 个子节点，`title` 必须 ≤ 12 字。
4. **方向约束**：`path_details` 仅用于校准思维方向与边界，不可直接摘录或复述其中描述。
5. **环境适配**：虽然进行结构化拆解，但词汇选择必须符合 `{{topic_constraints}}` 的专业语境。

# Output
1. 仅输出符合 Schema 的 JSON。
2. `logic_angle` 必须为 `Timeline` / `Architect` / `Microscope` 三者之一。
3. `insight`：一句话说明为何使用该模式，需体现当前深度或语义类型。
4. `hierarchy_check`：固定格式 `OK-<logic_angle>-depth-{{current_depth}}`。
